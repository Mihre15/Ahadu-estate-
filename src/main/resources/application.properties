spring.application.name=app
server.port=${PORT:8080}
jwt.secret=tjO6tTDe6EWXUOZdphDuqaW7H2bsv3PYzRwH6OTY+XYLzXMF64IF9P6CFVN4gK/A
jwt.expiration=86400000
# spring.datasource.url=${DB_URL:jdbc:postgresql://postgres:5432/ahadu_db}
# spring.datasource.username=${DB_USERNAME:ahadu_user}
# spring.datasource.password=${DB_PASSWORD:addis41}
spring.datasource.url=${DATABASE_URL}
# spring.datasource.url=jdbc:postgresql://${PGHOST}:${PGPORT}/${PGDATABASE}
# spring.datasource.username=${PGUSER}
# spring.datasource.password=${PGPASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.elasticsearch.uris=${ELASTICSEARCH_URIS:http://elasticsearch:9200}
logging.level.org.springframework.security=DEBUG
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true
spring.flyway.baseline-version=1

